// Tell SvelteKit to pre-render every page at build time.
// This is what makes the static export work - all pages become plain HTML files
// that can be served without any server-side runtime.
export const prerender = true;
